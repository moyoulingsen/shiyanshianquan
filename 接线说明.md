# LabGuard 接线说明

本文按当前 `firmware/sdkconfig` 和 `firmware/components` 源码汇总接线。

## ESP32-P4 GPIO 总表

说明：`I2C SDA` / `I2C SCL` 是 I2C 总线的两根信号线，分别是数据线和时钟线，不是单独的传感器名字。项目里这组 I2C 预留给 SHT3x / ENS160 使用。

| 功能 | ESP32-P4 引脚 |
|---|---|
| SHT3x / ENS160 I2C SDA | GPIO2 |
| SHT3x / ENS160 I2C SCL | GPIO3 |
| SC2336 SCCB SDA | GPIO7 |
| SC2336 SCCB SCL | GPIO8 |
| MQ-2 DO | GPIO4 |
| 风扇 MOSFET 控制 | GPIO5 |
| 水泵 MOSFET 控制 | GPIO6 |
| 演示灯光 5V 电源通断控制 | GPIO32 |
| WS2812 / SK6812 灯带数据 DI | GPIO33 |
| LCD 背光 BL/PWM | GPIO26 |
| LCD 复位 RST | GPIO54 |
| MAX98357A DIN | GPIO20 |
| MAX98357A BCLK | GPIO21 |
| MAX98357A LRC/WS | GPIO22 |

## 上电前必须检查

下面几项属于硬件致命风险，接错可能烧 ESP32-P4 GPIO、烧 MOSFET，或者让系统反复重启：

1. 所有接到 ESP32-P4 GPIO 的信号必须是 3.3V 逻辑，不能把 5V 信号直接接到 GPIO。
2. 外部 5V 负载电源、传感器电源、ESP32-P4 必须共地，也就是所有 `GND` 要连在一起。
3. 风扇、水泵这类电机负载必须有续流二极管或自带保护的驱动模块，否则关断瞬间的反向电压可能打坏 MOSFET 或干扰 ESP32-P4。
4. MOSFET 输入脚建议加下拉电阻，典型 `100K` 接到 GND，避免 ESP32-P4 刚上电还没初始化时负载误动作。
5. 灯带、风扇、水泵、功放、MQ-2 这类 5V 负载不要从 ESP32-P4 GPIO 取电，也不要超出开发板 5V 口/USB 口能提供的电流。

## SHT3x 温湿度传感器

当前固件把 SHT3x / ENS160 I2C 设为 `SDA=GPIO2`、`SCL=GPIO3`。这与 `firmware/sdkconfig`、`firmware/sdkconfig.defaults` 和 `firmware/components/sensor_reader/Kconfig` 当前配置一致。

按你这块板子右侧排针丝印，照片里可以直接找到 `GPIO2`、`GPIO3`、`GPIO4`、`GPIO5`、`GPIO6`、`GPIO7`、`GPIO8`、`GPIO20`、`GPIO21`、`GPIO22`、`GPIO26`、`GPIO32`、`GPIO33`、`GPIO54`。旧配置里用过的 `GPIO23`、`GPIO27` 没有出现在照片里，当前不再使用。

| SHT3x 引脚 | 接 ESP32-P4 |
|---|---|
| VCC | 3V3 |
| GND | GND |
| SDA | GPIO2 |
| SCL | GPIO3 |

## ENS160 空气质量传感器

当前固件启用 ENS160 的 I2C 总线；如果没有接 ENS160，系统会继续用默认演示数据运行。

注意：ENS160 和 SHT3x 这组 I2C 必须按 3.3V 总线使用。如果某个模块把 SDA/SCL 通过板载电阻上拉到 5V，不能直接接 ESP32-P4，需要拆掉 5V 上拉或加 I2C 电平转换器。

如果模块上标的是 `+ - C D`，按下面接：

| ENS160 标号 | 含义 | 接 ESP32-P4 |
|---|---|---|
| + | VCC | 3V3 |
| - | GND | GND |
| C | SCL | GPIO3 |
| D | SDA | GPIO2 |

如果模块上标的是 `VCC GND SCL SDA`，按下面接：

| ENS160 引脚 | 接 ESP32-P4 |
|---|---|
| VCC | 3V3 |
| GND | GND |
| SCL | GPIO3 |
| SDA | GPIO2 |

## MQ-2 烟雾/可燃气体传感器

当前固件读取 MQ-2 的数字报警脚 `DO`，源码配置是 `GPIO4`，并打开了内部上拉。`DO` 默认按低电平报警处理，也就是 `DO=0` 时上报 `mq2_alarm=true`；如果要上传真实模拟数据，必须把 `AO` 接到一个确认支持 ADC 的 ESP32-P4 引脚，并保证进入 MCU 的电压不超过 3.3V。当前代码已预留 AO 数值上报字段，但在 `AO` 没有实际接线前，系统仍只能得到数字报警。`GPIO4` 在你发的这张板子右侧排针丝印里可以直接找到。

重要：很多 MQ-2 模块用 5V 供电时，`DO` 会被板载比较器或上拉电阻拉到 5V。ESP32-P4 GPIO 不能直接承受 5V，所以 `DO -> GPIO4` 中间要加电平转换。最简单可以用电阻分压，例如 `DO -- 10K -- GPIO4 -- 20K -- GND`，把 5V 降到约 3.3V；也可以用专用电平转换器或开漏输出加 3.3V 上拉。

| MQ-2 引脚 | 接 ESP32-P4 |
|---|---|
| VCC | 5V |
| GND | GND |
| DO | 先降到 3.3V，再接 GPIO4 |
| AO | 经分压/调理后接 ADC 引脚（未接则无法上传真实数值） |

## MOSFET 模块、风扇、水泵、灯带

当前固件把 MOSFET 灯光电源控制脚设为 `GPIO32`，灯带数据脚设为 `GPIO33`。代码会在开灯时先拉通 `GPIO32` 对应 MOSFET 给灯带上电，再通过 RMT 在 `GPIO33` 输出 WS2812 / SK6812 兼容数据；关灯时会先发黑色数据，再关闭 `GPIO32` 电源控制。默认配置 `CONFIG_LABGUARD_ACTUATOR_LIGHT_LED_COUNT=8`，也就是先控制 8 颗灯珠；如果你的灯带实际灯珠数更多，需要把这个配置改成实际数量。

灯带照片上标的 `5V DI -` 按下面接：`5V` 接外部 5V 正极，`DI` 接 ESP32-P4 `GPIO33`，`-` 接 GND。灯带箭头方向是数据方向，数据线必须接到箭头起点那一端的 `DI`，不能接到另一端的 `DO`。如果灯带不稳定、不亮或颜色乱跳，在 `GPIO33 -> DI` 中间加 `74AHCT125` / `74HCT14` 这类 5V 供电的电平转换器，并在靠近灯带 `DI` 处串一个 `220~470Ω` 电阻。

灯带电源通断要特别注意 MOSFET 类型：很多廉价 MOSFET 模块是低边开关，只能断开负极/GND。WS2812 / SK6812 灯带的 `DI` 仍然连着 ESP32-P4 时，不建议断开灯带 GND，否则可能通过 `DI` 反向供电或导致异常发光。更稳的接法是：灯带 `-` 永久接公共 GND，用高边开关/负载开关去断 `5V`；或者灯带不断电，只通过 `GPIO33` 发黑色数据实现关灯。如果你只有低边 MOSFET 模块，优先把它用于风扇、水泵这类两线负载。

注意：`GPIO32` 只是 MOSFET 控制信号，不是 5V 供电脚。灯带、喇叭这类 5V 负载必须单独接外部 5V 电源正极，ESP32-P4 只能提供控制信号和 3.3V 逻辑电平，不能直接当负载电源用。如果你在“开启”时量到只有 4.2V，优先检查：

1. 负载的 `V+` 是否真的接到了稳定的 5V 电源，而不是 ESP32 板上的 GPIO 或经过 MOSFET 的输出端。
2. 外部电源和 ESP32 `GND` 是否共地。
3. 灯带/喇叭是否把启动电流拉得太大，导致 5V 适配器掉压到 4.xV。
4. MOSFET 模块是否是低边开关接法，`IN` 只是控制通断，不会直接输出 5V。

| MOSFET 模块引脚 | 接线 |
|---|---|
| IN1 | ESP32-P4 GPIO5 |
| IN2 | ESP32-P4 GPIO6 |
| IN3 / LIGHT | ESP32-P4 GPIO32 |
| GND | ESP32-P4 GND |
| V+ | 外部负载电源正极 |
| V- / GND | 外部负载电源负极 |

| 负载 | 接线 |
|---|---|
| 风扇 | 接 MOSFET IN1 对应通道，并确认有续流保护 |
| 水泵 | 接 MOSFET IN2 对应通道，并确认有续流保护 |
| 灯带 `5V` | 推荐接外部 5V 正极，并用高边开关/负载开关控制通断 |
| 灯带 `DI` / `D1` | 接 ESP32-P4 GPIO33 |
| 灯带 `-` / `GND` | 永久接外部 5V 负极，并与 ESP32-P4 GND 共地 |

## SC2336 摄像头

当前源码把摄像头控制总线配置为 `SCCB SDA=GPIO7`、`SCCB SCL=GPIO8`，走 `I2C_NUM_0`，和 SHT3x / ENS160 那组 I2C 不是同一组线。使用官方 ESP32-P4 Function EV Board 的 SC2336 摄像头子板时，`SDA/SCL/3V3/GND/MIPI-CSI` 都已经通过 `MIPI_CSI` 宽排线和转接板连接，不需要再把 `GPIO7`、`GPIO8` 额外飞线到排针。

| SC2336 信号 | 接 ESP32-P4 |
|---|---|
| 32-pin FPC | 主板 `MIPI_CSI` / `CSI` 接口 |
| SCCB SDA | FPC 内部连接到 GPIO7 |
| SCCB SCL | FPC 内部连接到 GPIO8 |
| 电源 / GND | FPC 内部连接，按官方摄像头子板方向插入 |

## EK79007 显示屏

当前源码按官方 ESP32-P4 Function EV Board HMI 子板接法配置为 `BL/PWM=GPIO26`、`LCD_RST=GPIO27`；图像数据走 `MIPI_DSI` 宽排线。注意 `GPIO54` 是无线协处理器相关复位脚，不能当 LCD reset 使用。

| 显示屏信号 | 接 ESP32-P4 |
|---|---|
| 32-pin FPC | 主板 `MIPI_DSI` / `DSI` 接口 |
| BL / PWM | GPIO26 |
| LCD_RST | GPIO27 |
| 5V | 5V |
| GND | GND |

## MAX98357A 音频功放

当前固件使用标准 Philips I2S，接线为 `DIN=GPIO20`、`BCLK=GPIO21`、`LRC/WS=GPIO22`。网页关闭声音时，固件会写入静音数据并关闭 I2S 输出，所以当前不需要额外连接 `SD / MODE` 控制脚。`SD / MODE` 可以保持模块默认接法；如果以后仍有底噪或上电瞬间杂音，再选择一个实际能接到的空闲 GPIO，启用 `CONFIG_LABGUARD_AUDIO_PROMPT_SD_MODE_GPIO` 做硬件静音。

注意：MAX98357A 的喇叭输出是桥接输出，喇叭两根线只接模块的 `+` / `-` 输出端，不要把喇叭任意一端接到系统 GND。功放如果用 5V 供电，电流要从外部 5V 电源提供，并和 ESP32-P4 共地。

| MAX98357A 引脚 | 接 ESP32-P4 |
|---|---|
| DIN | GPIO20 |
| BCLK | GPIO21 |
| LRC / WS | GPIO22 |
| VIN | 5V 或 3V3 |
| GND | GND |
| SD / MODE | 不接 ESP32 GPIO，使用模块默认 |
| GAIN | 不接，默认 9dB |

可选固定接法：

| MAX98357A 引脚 | 接法 | 效果 |
|---|---|---|
| GAIN | 不接 | 9dB |
| GAIN | 接 GND | 12dB |
| GAIN | 100K 电阻接 GND | 15dB |
| GAIN | 接 VIN | 6dB |
| GAIN | 100K 电阻接 VIN | 3dB |
| SD / MODE | 接 GND | 关闭功放 |
| SD / MODE | 使用模块默认 | 单声道混音输出 |
| SD / MODE | 100K 电阻接 VIN | 只输出左声道 |
| SD / MODE | 210K 电阻接 3V3 | 只输出右声道 |
| SD / MODE | 370K 电阻接 5V | 只输出右声道 |

## microSD

当前代码使用板载 `SDMMC` 四位总线，`app_main.c` 里直接走 `SDMMC_SLOT_CONFIG_DEFAULT()`，文档里继续按板载卡座理解即可，不建议再外接到前面这些通用 GPIO。

| 器材 | 接法 |
|---|---|
| microSD 卡座 | ESP32-P4 板载 SDMMC，默认 GPIO43/44/39/40/41/42 |
